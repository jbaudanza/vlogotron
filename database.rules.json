{
  "rules": {
    "video-clips": {
      "$uid": {
        ".read": true,
        ".write": "$uid === auth.uid"
      }
    },
    "queue": {
      "tasks": {
        ".read": true,
        ".write": true,
        ".indexOn": "_state"
      }
    }
  }
}