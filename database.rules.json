{
  "rules": {
    "video-clips": {
      "$uid": {
        ".read": true,
        ".write": "$uid === auth.uid"
      }
    },
    "video-clip-events": {
      "$uid": {
        ".read": true,
        ".write": "$uid === auth.uid"
      }
    }
  }
}